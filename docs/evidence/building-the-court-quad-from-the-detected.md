# Building the court quad from the DETECTED LINES (choose 2 baselines + 2 sidelines, intersect)

> Evidence for the `building-the-court-quad-from-the-detected` row in [docs/STATE.md](../STATE.md) (What has not worked).
> Text preserved verbatim from SCOREBOARD.md at the 2026-08-26 split.

**Wrong in principle, not mis-tuned.** It needs the detected lines split into two direction families, but **under perspective the two doubles sidelines converge, so they are not parallel in the image and form no angular cluster.** On `am_hard_utr` the split put 24 of 26 lines in one family while both true sidelines sat in the detected set at 0.1 and 2.2 px. Parameter-free double-angle clustering improved it and did not fix it. Best constructed quad **68–256 px** from truth vs the lattice's 7–20. `docs/archive/sessions/SESSION_P_search_reach.md` § P3.
