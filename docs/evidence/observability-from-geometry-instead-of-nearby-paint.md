# Observability from geometry instead of nearby paint (ev = seen, drop EVID_MIN)

> Evidence for the `observability-from-geometry-instead-of-nearby-paint` row in [docs/STATE.md](../STATE.md) (What has not worked).
> Text preserved verbatim from SCOREBOARD.md at the 2026-08-26 split.

**+0.000 margin on all 10 clips — byte-identical to shipped.** The one-line half is free and buys nothing, so the un-occluded half that needs real machinery has no evidence behind it. Closed.
