# Reachability — that bounded refinement cannot travel from its seed to the true court

> Evidence for the `reachability` row in [docs/STATE.md](../STATE.md) (What has not worked).
> Text preserved verbatim from SCOREBOARD.md at the 2026-08-26 split.

The pre-registered mechanism of Session P, **refuted by its own stopping rule**: on **31 of 38** clips the nearest seed is already within reach. The coarse-grid figure that motivated it was a lower bound — the full seed set is ~2,200 seeds, not 1,024. (7 clips *are* out of reach and a 640-normalised bound fixes all 7 — a real but small correction, not the main event.)
