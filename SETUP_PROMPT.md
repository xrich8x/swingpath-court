# Setup Prompt

**The repository is scoped to the court feature (2026-09-17).** The previous setup prompt is archived
at `swingpath:docs/archive/2026-09-17-pre-court-only/SETUP_PROMPT.md`.

To set up: create `backend/.venv` with Python 3.12, install `backend/requirements.txt`, then run
`python -m pytest tests/` from `backend/`. See `README.md` for the court tools.
