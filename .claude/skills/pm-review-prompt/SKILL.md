---
description: Print the standalone PM_REVIEW_PROMPT.md text into the terminal, verbatim, for copying elsewhere. Invoke with /pm-review-prompt.
disable-model-invocation: true
---

Read PM_REVIEW_PROMPT.md from the repo root and output its contents to the
chat now, inside a single fenced code block, exactly as written. No summary,
no commentary, no analysis before or after it — the point of this command is
a fast, literal terminal dump the user can select and copy, not a review.
