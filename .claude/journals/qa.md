# qa — working journal

**READ THIS FIRST IF YOU ARE RESTARTING.** A usage limit kills an agent outright and nothing restarts it
automatically. Whatever is below is what survived.

**SCOPE: the court feature only (founder, 2026-09-17).**

---

## TASK — 2026-09-19: audit G8 (far-line instrument + renderer fault)
Branch camera3d-pnp-paintfit. Python: "E:\Claude Outputs\Cowork Tasks\Swing Vision\backend\.venv\Scripts\python.exe".
Commits: prereg 46e5fe7, results f3bddd6. Tool tools/court_far_line_gate.py. Code camera3d.py, camtrack.py,
setup_state.py, tools/court_track_sim.py, tools/court_fit_cp1.py.
9 claims: (1) far lines observable/checked frac 1.000; (2) held-out 400-402 catch .9167 @0 false — is it a
held-out SCENE or just noise draw?; (3) BAR3 both silent wrong locks caught, one-variable A/B?; (4) BAR4 fails
369/369, attributed to CLUTTER not lens; ships OFF, pre-G8 reproducible?; (5) FAR_TOL_PX_720=0.75 noise-only
sweep 1728 combos seeds 300-305, far_min_z inert 3-8; (6) RENDERER FAULT blast radius — QUALIFIED/OVERTURNED/
UNAFFECTED for G3, dev seeds 100-102/200-202, 3.5cm & 2.41cm — MY judgement; (7) X265_DETERMINISTIC identical
vs null 28 fields, NOT comparable w/ CP1/G1/G7; (8) honest lock semantics — try to make a file out-claim scope;
(9) backend-dev's own prediction wrong (pyramid 1.000 vs 0.917), arm chosen defensibly?
Also: suite 414 pass/10 skip/2 pre-existing fail (prev 373/10/5).
Deliverable: append "QA AUDIT 2026-09-19: G8, the far-line instrument and the renderer fault" to
docs/evidence/court-camera3d.md + STATE row if material. Commit locally, DO NOT PUSH.

## STATE
- Started. Prior task (G7 audit) DONE, committed 1f377af.

## LOG
- 2026-09-19 start. git log confirms prereg 46e5fe7 BEFORE results f3bddd6; 4936d28 + 2dc86ae are journal/memory only.
- ARTIFACTS: all 5 G8 JSONs stamped commit 46e5fe7 (the PREREG commit) => run after prereg, before results commit f3bddd6. cost_separation stamp dirty:true. Artifact mtimes 12:35-12:58, prereg 12:05, results 13:18. Chronology CLEAN.
- SWEEP REPRODUCES EXACTLY from raw rows. DEV stack: tol .10-.35 catch 1.000; .50 .9792/false .0159; .75 .9167/.0000; 1.00 .8472/.0000. falseTot .442/.370/.261/.217/.130/.1014/.0870/.0870. Matches the evidence table to 4dp. choose() rule correctly applied: .50 excluded by false .0159>.01 => .75 chosen. HELD-OUT tol .75: catch 66/72=.9167, false 0/63, falseTot 6/69=.0870. BAR1 PASS, BAR2 PASS.
- z-INERT claim: TRUE for tol .10-.75 (all 5 z identical). FALSE at tol 1.00: z=8 gives 123/144 vs 122/144 at z=3-6. Evidence says "every row above is identical at z=3,4,5,6,8" - that row is not. Tiny, does not change the choice (1.00 loses on catch either way).
- **LADDER GEOMETRY IS IDENTICAL ACROSS ALL SEEDS.** 57 ladder cases/seed; far_err per (motion,mag) has exactly 1 distinct value within dev AND within heldout, and the dev<->heldout maps are EQUAL (57/57 keys). cases() renders ONE frame per seed from the TRUE camera; only the noise realisation changes. => seeds 400-402 are a held-out NOISE DRAW, not a held-out scene. CLAIM 2 concern CONFIRMED, stronger than stated.
- **EFFECTIVE n IS 24, NOT 72.** 72 wrong rows = 24 distinct geometries x 3 seeds. The 6 heldout misses are the SAME 2 geometries (pitch +/-0.05 deg, 25.82/26.27 cm) on all 3 seeds - and they are the SAME 2 that miss on all 6 dev seeds. 22/24 distinct = .9167. One more distinct miss -> .875 = BAR 1 FAIL. Margin = ONE geometric case.
- **THE WHOLE GAIN OVER PRE-G8 IS 2 DISTINCT CASES.** preG8 misses exactly 4 distinct pitch cases (+/-0.05 = 25.8/26.3 cm; +/-0.1 = 51.2/53.0 cm), all pitch, confirmed. Stacked catches the +/-0.1 pair only. Pyramid catches all 4. So .833 -> .917 = pure pitch ~0.1 deg, and .833 -> 1.000 for the pyramid.
- Band table reproduces exactly (57/24/18/18/18/36 with .105/.500/.333-.667/.667/.667-1.000/1.000).
- DEV tol 0.50's 2 incremental false flags = seeds 302,303 height +0.01 (9.92 cm worst) - i.e. cameras 9.9 cm out, just inside the 10 cm GOOD line. The .50->.75 step is decided by 2 borderline-good rows.
- 15/171 heldout rows still have an UNCHECKED far line under the stack - all gross (pitch 0.6 deg, height +/-0.6/1.5 m, 2.8-14.9 m error), all ok=False anyway. far_unchecked_on_good = 0.0.
- CLAIM 3 A/B VERIFIED ONE-VARIABLE: full structural diff of seeds101-201_n120_sub_far.json vs _sub.json = **12 differing leaves total**, and they are exactly: far_lines flag x2, paint_check_far_lines x2, locked[60] x2 (in 2 places = 4), and 4 summary aggregates. Every per-frame line error, jump and recovery figure BIT-IDENTICAL. locked 240->238, locked_wrong 2->0, unlocked 0->2, knock_recovery [1,1] both. Frame 60 far_baseline 0.62270 m (seed 101) / 0.49115 m (seed 201) - matches 62.27/49.11 cm. BAR 3 CONFIRMED.
- CLAIM 4 nondegrade REPRODUCES from rows: 31 wrong/369 right by the focal rule; far ON catch 31/31 false 369/369; far OFF catch 31/31 false 2/369 (trials 32 and 154, both worst=centre_service). ceiling_true_camera.ok_rate 0.000. Worst line on the 369 flagged right cameras: far_service 368, far_baseline 1 => the flag is the far instrument, confirmed. far_service frac==0.0 on 384 of 389 seen.
- CLAIM 4 minor numeric slips: evidence says far_baseline unchecked 400/400 (actually **394/400**, 6 checked) and far_service checked 394/400 (actually **389/400**). Direction unchanged.
- CLAIM 4 CAVEAT FOUND: the code comment says "support stays WIDE-sample only, so it remains the same number G7 scored". With far lines ON it does NOT: support moved on **4 of 400** rows, max 0.01136 (far_service joins `lines`, so its WIDE samples join the `checked` mask). Immaterial while the instrument ships OFF, but the comment is wrong as written.
- **RULE-7 NO-OP CONFIRMED INDEPENDENTLY** (my own control, git worktree at 46e5fe7 vs HEAD): render() at default args gives a **byte-identical image (sha256 7340fd1d...)**; paint_check at default args identical on 3 cameras (lines/support/worst/unchecked); S.run(seed=101,n=6) identical on setup_worst_m, status, locked, all per-frame line errors and jumps. Exactly ONE difference in the whole comparison: the new TrackConfig.far_lines field. So SS_DEFAULT/SUBPIXEL_DEFAULT really do leave the pre-G8 path untouched.
- INCONSISTENCY: court_track_sim comment says "G8 scores the far-line instrument at ss=4", but every G8 artifact stamp says ss=2 (subpixel=true). Comment is wrong; the runs are at ss=2.
- SUITE CONFIRMED at HEAD: **414 passed, 10 skipped, 2 failed** in 108 s; the 2 are test_recording_identity.py::test_the_known_alias_resolves_to_one_recording and ::test_overlap_is_reported_not_silently_merged. Matches the claim. (Pre-G8 worktree suite still running for the 394 delta.)
- **CLAIM 7 CONFIRMED INDEPENDENTLY (encoder level, my own control).** (a) I reconstructed the pre-G8 ffmpeg argv literal by hand: `_encode_argv(X265, fn)` is CHARACTER-IDENTICAL => the CP1 path is a proven no-op. (b) 4 encodes of ONE byte-identical 20-frame array: cp1 -> **4 distinct decoded sha256**, kbps 22414.9-22504.4, mean spread 0.022 DN; deterministic -> **1 hash**, kbps identical, spread 0 DN. Determinism holds; the null direction (cp1 differs) also holds.
- **NEW NUMBER, not in the writeup:** on that array the deterministic profile encodes at **325,033 kbps vs cp1's 22,461 - 14.5x the bitrate**. That is the starkest available evidence that the two profiles are different scenes, and the writeup never gives the deterministic profile's bitrate at all (it lists only cp1's 20620/19498).
- **CLAIM 8: re-derivation WORKS, but there is a hole.** normalize_camera IS on the real read path (normalize() calls it when setup.camera present; to_dict() splats extra so lock_scope/lock_claim travel). Forged lock_claim TEXT is overwritten (attack B defeated); unknown / absent / list-valued scope all read `unknown`, never whole_court (E/F/G defeated); bare-string lock_unverified degrades safely to "some lines" (H). **BUT attack C succeeds:** hand-setting `lock_scope: "whole_court"` while `lock_unverified` STILL lists far_baseline/far_service yields "Every court line was checked against the paint." Nothing cross-checks scope against lock_unverified. The claim never exceeds the SCOPE, but the scope itself is forgeable and self-contradictory files are accepted. Not a gate (metrics_eligible does not consult it), so QUALIFIED not P0.
- **P0-CLASS FINDING A - THE COMMITTED TOOL DOES NOT RUN THE INSTRUMENT.** tools/court_far_line_gate.py line ~"c = camera3d.paint_check(img, cam, far_kw={...})" omits `far_lines=True`, and FAR_LINES_DEFAULT=False at HEAD. I verified by invoking it: the tool-style call returns detail keys with NO far line at any tol; only `far_lines=True` engages it. So re-running the committed tool today gives the PRE-G8 baseline (catch 0.8333 flat) in all 40 cells - it cannot reproduce its own committed sweep. The artifacts must have been produced with FAR_LINES_DEFAULT=True in the working tree, flipped to False before the results commit without updating the tool. Numbers not wrong; provenance broken.
- **P0-CLASS FINDING B - UNDECLARED DEVIATION FROM THE PRE-REGISTRATION, AND IT IS WHAT MAKES BAR 4 FAIL.** Prereg (evidence line 897): "reach = 3 * far_tol" => 2.25 px@720 at tol 0.75. Shipped code: `reach_px_720: float = 8.0` FIXED - **3.56x wider**. The results section declares only ONE deviation (the peak rule) and never mentions this. The net tape sits 4.8-5.6 px from the far service line: INSIDE the shipped window, OUTSIDE the pre-registered one.
  - My control, CP1 arm-P trials 0/1/2, the EXACT rendering camera: reach 8.0 -> paint_check.ok=**False**, worst=far_service, stacked peak at **-4.85 to -5.42 px** (the tape). reach 2.25 -> far lines go `seen=False` => `unchecked`, and **paint_check.ok=True**. The true camera PASSES at the pre-registered reach on all 3 trials.
  - Held-out ladder re-scored by MY OWN reimplementation: reach 8.0 -> catch **66/72 = 0.9167**, false 0/63 (EXACTLY reproduces the artifact); reach 2.25 -> catch **65/72 = 0.9028**, false 0/63 (still over the 0.90 bar, by one frame; extra distinct miss pitch -0.1).
  - => BAR 1/2 survive the pre-registered reach; **BAR 4's "369 of 369, the rendering camera included" does NOT** - at the pre-registered reach the instrument is INERT on CP1's scene, not hostile. The clutter ATTRIBUTION is confirmed (the peak really is the tape), but "the net tape defeats the instrument" is conditional on the undeclared widening.
- **CLAIM 6 BLAST RADIUS - MY PAIRED ONE-VARIABLE CONTROL.** Same seeds 101+201, n=120, only the render order: worst-line p90 **2.595 cm (pre-G8) -> 1.479 cm (fixed)** = 1.76x; steady jump 0.178 -> 0.126 px; far_baseline p90 2.81/2.43 -> 1.52/1.43 cm; knock frames **70.39/56.82 cm -> 62.27/49.11 cm** (the 70.4/56.8 I reported on 2026-09-18 reproduce EXACTLY); locked_wrong 2 in both; lock_step baseline 13.54 -> 12.65 m. VERDICT: **QUALIFIED, nothing overturned.** G3's KILL is UNAFFECTED (11-13 m, not a 1.8x artefact). 3.5 cm / 2.41 cm are renderer-conditional and PESSIMISTIC by ~1.8x.
- **WIDER THAN backend-dev SAYS:** the fault is NOT confined to the far lines. The WIDE near_service line's stacked offset moves -0.19 px (shipped) -> -0.09 px (subpixel). That is why the whole-court p90 moved 1.76x. "those runs measured a tracker whose far lines were effectively absent" understates it.
- My own renderer-fault numbers (noiseless, true cam, median over segments, global argmax): far_baseline amp 0.27 / 11.35 / 5.68 DN at ss 2/4/8 (NON-MONOTONE - supersampling is not a fix), far_service 22.38 DN at ss=2 => **83x asymmetry between two lines that should read the same**. subpixel ss=2: 5.88 / 8.79 DN, offsets -0.000 / +0.024. With the SHIPPED _seg_hit rule + noise at ss=2 non-subpixel the far_baseline offsets are **+9.11 and +6.79 px** (backend-dev reported +3.96) and frac 0.0. Mechanism confirmed emphatically; the exact DN/offset figures are protocol-dependent, so do not quote 0.49 DN / +3.96 px as exact.
- CLAIM 1 CONFIRMED by my own run: seeds 400/401, far_baseline + far_service both frac **1.000**, seen, scope **whole_court**, at native 1080p AND 4K rendered + INTER_AREA downscale to 1080p. Caveats: n_seg is **6 and 4**, not the pre-registered 8 (capped by len(idx)//min_samples); "offset within 0.05 px" is the NOISELESS figure - with sensor noise per-segment offsets reach +0.094 px.
- SUITE DELTA CONFIRMED: pre-G8 worktree 391 pass/10 skip/5 fail; the 3 extra failures are test_refs_pool_strict16.py (needs data/incoming clips, absent in a worktree) => 391+3 = **394**, and 414-394 = **20** = test_far_line_check(14) + test_codec_profile(6). Exactly as claimed.
- Evidence section appended to docs/evidence/court-camera3d.md (12-claim table + 12 subsections + 2 STATE rows as TEXT ONLY; docs/STATE.md NOT touched). Worktree removed. Committing next.

## STATE (final)
- COMPLETE 2026-09-19. Verdicts: 1 CONFIRMED(2 corrections) / 2 CONFIRMED-arithmetic,QUALIFIED-evidence / 3 CONFIRMED / 4 QUALIFIED (undeclared reach deviation; "ships OFF" no-op CONFIRMED) / 5 CONFIRMED w/ exception / 6 QUALIFIED, larger than stated, nothing overturned / 7 CONFIRMED independently / 8 CONFIRMED w/ hole / 9 CONFIRMED. Suite 414/10/2 CONFIRMED.
- TWO PROCESS FAULTS TO HAND TO THE LEAD: (a) tools/court_far_line_gate.py omits far_lines=True => cannot reproduce its own sweep at HEAD; (b) reach_px_720=8.0 vs pre-registered 3*far_tol=2.25 - undeclared, and it is what puts the net tape inside the search window. Neither fixed by me.
